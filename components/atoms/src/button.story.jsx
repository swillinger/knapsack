import React from 'react';
import { Button } from './button';

export default {
  title: 'Components|Atoms/Button',
  component: Button,
  decorators: [],
  parameters: {},
};

export const simple = () => <Button>I am a Button!</Button>;
