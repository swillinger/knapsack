{
  "name": "@knapsack/schema-table",
  "license": "GPL-2.0-or-later",
  "version": "0.45.0",
  "description": "A Knapsack component that helps build Design Systems",
  "module": "dist/schema-table.js",
  "main": "dist/schema-table.js",
  "scripts": {
    "build": "babel --config-file @knapsack/babel-config/es --copy-files --source-maps --out-dir dist/ --delete-dir-on-start ./src/",
    "prepublishOnly": "yarn build",
    "watch": "yarn build --watch"
  },
  "dependencies": {
    "@knapsack/core": "^0.45.0",
    "react-table": "^6.8.6"
  },
  "devDependencies": {
    "prop-types": "^15.0.0",
    "react": "^16.8.0"
  },
  "peerDependencies": {
    "prop-types": "^15.0.0",
    "react": "^16.8.0",
    "styled-components": "^4.0.2"
  },
  "files": [
    "./dist"
  ],
  "author": "Basalt (https://basalt.io/)",
  "publishConfig": {
    "access": "public"
  }
}
