{
  "name": "<%= name %>",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production webpack-cli -p --mode production",
    "serve": "node api-server/index.js",
    "start:api": "nodemon --watch api-server/ --exec 'yarn serve' ",
    "start:webpack": "DEV_MODE=yes webpack-dev-server",
    "start": "run-p --print-label start:api start:webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@basalt/bedrock-api-server": "latest",
    "@basalt/bedrock-pattern-manifest": "latest",
    "@basalt/bedrock-site": "latest",
    "@basalt/demo-data": "^0.5.1",
    "@basalt/twig-renderer": "^0.10.2"
  },
  "author": "",
  "license": "UNLICENSED"
}
