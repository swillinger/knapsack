{
  "name": "@knapsack/renderer-react",
  "version": "1.8.0",
  "description": "Render React",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "BABEL_ENV=cli babel --config-file @knapsack/babel-config --extensions .js,.ts --out-dir dist/ src/",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "yarn build --watch"
  },
  "dependencies": {
    "camelcase": "^4.1.0"
  },
  "peerDependencies": {
    "@basalt/knapsack": "^1.0.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "webpack": "^4.0.0"
  },
  "author": "Basalt (https://basalt.io/)",
  "publishConfig": {
    "access": "public"
  }
}
