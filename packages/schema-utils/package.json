{
  "name": "@knapsack/schema-utils",
  "license": "GPL-2.0-or-later",
  "version": "2.0.0-beta.0",
  "description": "A Knapsack component that helps build Design Systems",
  "main": "dist/schema-utils.js",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "babel --config-file @knapsack/babel-config --source-maps --ignore 'src/*.test.js' --extensions .js,.ts --out-dir dist/ src/",
    "prepublishOnly": "yarn build",
    "start": "yarn build --watch"
  },
  "dependencies": {
    "ajv": "^6.6.1",
    "ajv-keywords": "^3.4.1"
  },
  "author": "Basalt (https://basalt.io/)",
  "publishConfig": {
    "access": "public"
  }
}
