{
  "name": "@knapsack/schema-utils",
  "license": "GPL-2.0-or-later",
  "version": "1.5.2",
  "description": "A Knapsack component that helps build Design Systems",
  "main": "dist/schema-utils.js",
  "scripts": {
    "build": "babel --config-file @knapsack/babel-config --copy-files --source-maps --out-dir dist/ --delete-dir-on-start ./src/",
    "prepublishOnly": "yarn build",
    "start": "yarn build --watch"
  },
  "dependencies": {
    "ajv": "^6.6.1"
  },
  "author": "Basalt (https://basalt.io/)",
  "publishConfig": {
    "access": "public"
  }
}
