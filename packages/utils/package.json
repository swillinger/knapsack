{
  "name": "@basalt/bedrock-utils",
  "version": "0.15.1-alpha.1",
  "description": "A Bedrock component that helps build Design Systems",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "build:js": "babel --config-file @basalt/bedrock-babel-config/es --copy-files --source-maps --ignore 'src/*.test.js' --extensions .js,.ts --out-dir dist/ src/",
    "build:types": "tsc",
    "build": "rm -rf dist && yarn build:js",
    "watch": "yarn build:js --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@basalt/bedrock-core": "^0.15.0",
    "color": "^3.0.0"
  },
  "devDependencies": {
    "@basalt/bedrock-babel-config": "^0.15.0"
  },
  "author": "Basalt (https://basalt.io/)",
  "publishConfig": {
    "access": "restricted"
  }
}
